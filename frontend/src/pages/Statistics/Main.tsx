import React, { useState } from 'react';
import './Main.scss';
import { SurpriseResponse } from 'types';

import Page from 'components/Page';
const Main = () => {
    setSurprise(values);

    return <Page title="Statistics"></Page>;
};

export default Main;
